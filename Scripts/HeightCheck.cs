using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//Script to hold information about the furniture UI, whether it is up or down and the original height it should go back to
public class HeightCheck : MonoBehaviour
{
    // Start is called before the first frame update
    Transform currentHeight, originalHeight;

    public bool down;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }


}
