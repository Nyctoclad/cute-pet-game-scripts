using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

//Script that handles a confirmation box pop-up
public class ConfirmBoxInfo : MonoBehaviour
{
    public Text descriptionText, title;
    public Button confirm, exit;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
