using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//Friendship information about other pet and that pet's owner

public class PetFriendship : MonoBehaviour
{
    public Animal petFriend;
    public float petOwnerUserID; //User ID of pet owner
    public float friendshipAmount; //How attracted are the pets to each other
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
